[package]
name = "rl-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "rl-engine tier 0: grid, geometry, seeds, dice, ids and the turn queue. No Bevy."
homepage.workspace = true
keywords = ["roguelike", "gamedev", "grid", "rng", "turn-based"]
categories = ["game-development", "data-structures"]

[package.metadata.rl-engine]
# Read by scripts/check-tiers.sh: 0 core, 1 no Bevy, 2 the Bevy layer, 3 the facade and games.
tier = 0

[dependencies]
rand = { workspace = true, features = ["std", "std_rng"] }
serde.workspace = true

[dev-dependencies]
ron.workspace = true
